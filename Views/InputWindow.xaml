<Window x:Class="ExCord.Views.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ExCord Input"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="620"
        Height="120"
        WindowStartupLocation="Manual">

    <Border Background="#EE1E1F22" CornerRadius="18" BorderBrush="#5865F2" BorderThickness="1.5" Padding="16,12">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="4" BlurRadius="12" Opacity="0.4"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="2,0,0,6">
                <Ellipse Width="8" Height="8" Fill="#5865F2" Margin="0,0,6,0"/>
                <TextBlock Text="Text Input" Foreground="#949BA4" FontSize="11" FontWeight="Bold"/>
            </StackPanel>

            <TextBox x:Name="InputTextBox"
                     Grid.Row="1"
                     Background="Transparent"
                     BorderThickness="0"
                     Foreground="#F2F3F5"
                     FontSize="20"
                     FontWeight="Medium"
                     VerticalContentAlignment="Center"
                     TextWrapping="NoWrap"
                     AcceptsReturn="False"/>
        </Grid>
    </Border>
</Window>